# Z-CMDB Lite 环境变量配置示例
# 复制此文件为 .env 并按需填写
#
# 初始管理员密码（首次启动时生效，留空则随机生成）
# 设置后 Docker 与 Windows 两种启动模式将使用相同初始密码
CMDB_INITIAL_ADMIN_PASSWORD=

# refresh cookie Secure 标志
# 纯 HTTP 内网部署设为 false（默认），启用 HTTPS 时设为 true
COOKIE_SECURE=false
